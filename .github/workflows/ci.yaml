name: CI
on:
  workflow_dispatch:
  push:
    branches:
      - main
    paths-ignore:
      - "*"
      - "**.md"
      - "!flake.nix"
      - "!flake.lock"
  pull_request:
    types: [opened, synchronize, labeled, reopened]
    paths-ignore:
      - "*"
      - "**.md"
      - "!flake.nix"
      - "!flake.lock"
defaults:
  run:
    shell: bash
env:
  CACHIX_BINARY_CACHE: cameronraysmith
jobs:
  nixci:
    concurrency:
      group: nixci-${{ matrix.os }}-${{ github.workflow }}-${{ github.event_name == 'pull_request' && github.event.pull_request.number || github.ref_name }}
      cancel-in-progress: true
    strategy:
      matrix:
        os:
          # - macos-latest
          - ubuntu-latest
    runs-on: ${{ matrix.os }}
    steps:
      - name: Maximize build space
        uses: cameronraysmith/maximize-build-space@c28619d8999a147d5e09c1199f84ff6af6ad5794 # ratchet:cameronraysmith/maximize-build-space@print-usage
        with:
          build-mount-path: /nix
          build-mount-path-ownership: root:root
          root-reserve-mb: 34816
          swap-size-mb: 1024
          remove-dotnet: "true"
          remove-android: "true"
          remove-codeql: "true"
          remove-docker-images: "true"
      - uses: cachix/install-nix-action@7be5dee1421f63d07e71ce6e0a9f8a4b07c2a487 # v31
        with:
          nix_path: nixpkgs=channel:nixos-unstable
      - uses: cachix/cachix-action@0fc020193b5a1fa3ac4575aa3a7d3aa6a35435ad # v16
        continue-on-error: true
        with:
          authToken: ${{ secrets.CACHIX_AUTH_TOKEN }}
          name: ${{ env.CACHIX_BINARY_CACHE }}
      - uses: actions/checkout@08c6903cd8c0fde910a37f88322edcfb5dd907a8 # v5
      - run: |
         nix develop --command om ci run --systems "x86_64-linux"
      - run: df -h
