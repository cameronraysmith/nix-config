## 1. Module configuration

- [x] 1.1 In `modules/machines/nixos/pyrite/default.nix`, set `services.displayManager.gdm.autoSuspend = false` beside the existing `services.displayManager.gdm.enable` — verify: `nix eval .#nixosConfigurations.pyrite.config.services.displayManager.gdm.autoSuspend` returns `false`, and `nix eval .#nixosConfigurations.pyrite.config.programs.dconf.profiles.gdm.databases --apply builtins.length` returns one more entry than it does before the change, which is the `lib.optionals (!cfg.autoSuspend)` branch of nixpkgs `gdm.nix:337-347` becoming non-empty.
  - Evidence: `nix eval .#nixosConfigurations.pyrite.config.services.displayManager.gdm.autoSuspend` → `false`. `... .programs.dconf.profiles.gdm.databases --apply builtins.length` → `2`; `... .services.displayManager.gdm.banner` → `null`, so the banner `lib.optionals` contributes 0 and the 2 entries are exactly the `!autoSuspend` database plus the trailing `"${gdm}/share/gdm/greeter-dconf-defaults"` — i.e. 1 before the change, 2 after.
  - Evidence (content, not count): evaluating `lib.generators.toDconfINI` over the actual gdm profile databases yields, as the **first** and therefore highest-priority database, ahead of `/nix/store/gvd7k6m9zr7p5w6q911a3fk3pza1834f-gdm-50.1/share/gdm/greeter-dconf-defaults`:

    ```
    [org/gnome/settings-daemon/plugins/power]
    sleep-inactive-ac-timeout=0
    sleep-inactive-ac-type='nothing'
    sleep-inactive-battery-timeout=0
    sleep-inactive-battery-type='nothing'
    ```

- [x] 1.2 In the existing `programs.dconf.profiles.user.databases` block, change `sleep-inactive-battery-timeout` from `lib.gvariant.mkInt32 1800` to `lib.gvariant.mkInt32 0`, and add `sleep-inactive-ac-type = "nothing"` and `sleep-inactive-battery-type = "nothing"`. Leave `idle-delay = lib.gvariant.mkUint32 1800` and the absent `locks` attribute untouched — verify: `nix eval .#nixosConfigurations.pyrite.config.programs.dconf.profiles.user.databases` shows the four power keys with those values and `idle-delay` unchanged, and the evaluation does not throw, since `lib.gvariant.mkValue` refuses to infer an integer width from a bare Nix integer during keyfile generation rather than at the option's type check.
  - Evidence: the option eval is necessary but not sufficient — it shows the Nix-level attrset, not the GVariant text that `dconf compile` consumes. The generated keyfile was therefore read **out of the store**. `programs.dconf.packages` resolves the `user` profile to `/nix/store/p0xbl05h331rgwahh6ncakz0ig1kqlw1-user.drv`; walking `nix derivation show -r` on it gives the keyfile input `/nix/store/dni7zhw54yf1sl6q2rjhvkjxdyzqai9j-nixos-generated-dconf-keyfiles`, already realised locally. Its sole file `nixos-generated-dconf-keyfiles` reads verbatim:

    ```
    [org/gnome/desktop/session]
    idle-delay=@u 1800

    [org/gnome/settings-daemon/plugins/power]
    sleep-inactive-ac-timeout=0
    sleep-inactive-ac-type='nothing'
    sleep-inactive-battery-timeout=0
    sleep-inactive-battery-type='nothing'
    ```

    All five keys are present. The bare Nix strings render as **quoted** GVariant strings `'nothing'`, which is the correct serialization for an enum key — the schema at `gnome-settings-daemon` tag `50.1`, `data/org.gnome.settings-daemon.plugins.power.gschema.xml.in:19,29`, declares both `-type` keys `enum="org.gnome.settings-daemon.GsdPowerActionType"` with `<default>'suspend'</default>`, the same quoted form. `nothing` is a valid nick: `data/gnome-settings-daemon/gsd-enums.h:94-103` defines `GSD_POWER_ACTION_NOTHING`, and `data/meson.build:33-39` runs `gnome.mkenums` with `vprod: '<value nick="@valuenick@" .../>'`, so the nick is `nothing`. `idle-delay` is unchanged at `@u 1800`; no `locks` file content is contributed by this database.
  - Evidence (compiled output, not just the generator input): the built system's `/etc/dconf/profile/user` is `user-db:user` then `file-db:/nix/store/xffqrfxrqgs2p8h8bqsf6cqsbzzc72y3-dconf-db`. Decoding that compiled GVDB database with the real tool — `DCONF_PROFILE=<file-db-only profile> nix run nixpkgs#dconf -- dump /` — yields:

    ```
    [org/gnome/desktop/session]
    idle-delay=uint32 1800

    [org/gnome/settings-daemon/plugins/power]
    sleep-inactive-ac-timeout=0
    sleep-inactive-ac-type='nothing'
    sleep-inactive-battery-timeout=0
    sleep-inactive-battery-type='nothing'
    ```

    `dconf` round-trips the enum keys as GVariant strings and `idle-delay` as `uint32 1800`, so the stored values are well-formed rather than merely well-formed on input. The same dump against the gdm profile's `file-db:/nix/store/r4clhhgiag7ndgn7vaka42sqisy0hb5p-dconf-db` returns the four power keys with the identical values.
- [x] 1.3 Extend the comment above that block to record why automatic suspend is off: roughly one resume in five fails with no identified cause (`logs/pyrite-resume-failure-diagnosis.md` §1.3, 7 failures / 30 successes across 14 boots), the `disable-lid-wakeup` unit leaves the power button as the machine's only wake source (`logs/pyrite-resume-failure-diagnosis.md` §5.2), so a failed unattended resume costs a physical trip; and note that the `-type` keys are what actually disarm the idle watch, per gnome-settings-daemon's `idle_configure()` guard. State that this is harm reduction and not a fix — verify: reading the file shows the comment naming the failure rate, the sole wake source, and the harm-reduction framing, and `just lint` passes.
  - Evidence: the comment names the 7/30-across-14-boots failure rate, names `disable-lid-wakeup` leaving the power button as the only wake source, and says in as many words "This is harm reduction, not a fix".
  - **Correction made to the prior draft of this comment.** The draft asserted that "nothing" "registers no watch at all and survives someone later setting a nonzero timeout, whereas a 0 timeout only avoids firing a watch that is still armed". The second clause is false. At tag `50.1`, `plugins/power/gsd-power-manager.c:2102` opens `if (timeout_sleep != 0) {` and the type guard at `:2105`, `if (action_type != GSD_POWER_ACTION_NOTHING) {`, is **nested inside** it; `:2082-2085` also `clear_idle_watch`es both ids unconditionally beforehand. So a 0 timeout registers no watch either — it does not leave an armed watch that merely declines to fire. The comment now states that a 0 timeout and a `"nothing"` type each independently prevent registration, and that the `-type` keys are kept because they still hold if a nonzero timeout is later written back into the user database (which is what GNOME Settings did on 2026-09-08).
  - The draft also glossed the `-type` keys as `enum-typed ("s")`; the comment now cites the actual schema `enum=` attribute and default, and states that the claim was verified against the generated keyfile rather than the source.
  - Formatting: `treefmt --fail-on-change --no-cache modules/machines/nixos/pyrite/default.nix` → `formatted 1 files (0 changed)`. Full `just lint` was **not** run: it is `prek run --all-files`, and other agents currently hold in-flight edits to `openspec/changes/stand-up-gitea-mq-on-magnetite/proposal.md` and `terraform/terraform.tfstate*` in this same working copy, so an `--all-files` formatter pass could rewrite files this change does not own. The treefmt hook was run scoped to the one file this change touches; gitleaks is a `--staged` hook with nothing staged.
- [x] 1.4 Confirm nothing else moved — verify: `git diff --stat` reports `modules/machines/nixos/pyrite/default.nix` as the only non-`openspec/` file changed, and `git diff` on it shows no edit to `services.logind.settings.Login`, to `disable-d3cold-all`, to `nvme-d3cold-suspend-guard`, to `disable-lid-wakeup`, or to `boot.kernelParams`.
  - Evidence: the whole-tree form of this check cannot pass as literally written, because the shared working copy also carries other agents' concurrent edits to `.atomic/workflows/gitea-mq/dns-checks.mjs`, `openspec/changes/stand-up-gitea-mq-on-magnetite/proposal.md`, `terraform/terraform.tfstate` and `terraform/terraform.tfstate.backup`. Those are not this change's and were not touched.
  - Scoped to this change's file, `jj diff -- modules/machines/nixos/pyrite/default.nix` has exactly four non-comment changed lines:

    ```
    +      services.displayManager.gdm.autoSuspend = false;
    -              sleep-inactive-battery-timeout = lib.gvariant.mkInt32 1800;
    +              sleep-inactive-battery-timeout = lib.gvariant.mkInt32 0;
    +              sleep-inactive-ac-type = "nothing";
    +              sleep-inactive-battery-type = "nothing";
    ```

    Every other changed line is a comment. No hunk touches `services.logind.settings.Login`/`HandleLidSwitch*`, `disable-d3cold-all`, `nvme-d3cold-suspend-guard`, `disable-lid-wakeup`, `boot.kernelParams`, `idle-delay`, or adds a `locks` attribute. Nothing is masked and no `AllowSuspend`/`IdleAction` was introduced.

## 2. Deployment

- [x] 2.1 Instantiate pyrite's configuration before deploying — verify: `nix eval .#nixosConfigurations.pyrite.config.system.build.toplevel.drvPath 2>&1 | tee logs/nixos-pyrite-eval-$(date +%Y%m%d-%H%M%S).log` succeeds, walking the whole host configuration for option collisions and assertions. Note that evaluation proves what the generated dconf database will contain and proves nothing about what `gsd-power` reads, because a second database sits ahead of it; that is task 3.
  - Evidence: eval succeeded → `"/nix/store/n1gkzg2w7x5zjsg5ljr0y68lqfk9pvvl-nixos-system-pyrite-26.11.20260804.85f6261.drv"` (log: `logs/nixos-pyrite-eval-*.log`). Beyond instantiation, `nix build .#nixosConfigurations.pyrite.config.system.build.toplevel --no-link` was run and **succeeded**, on the `ssh-ng://builder@magnetite` remote builder, producing `/nix/store/5wm4fdmvdjcpyw5xppqkpnwskhbl0syi-nixos-system-pyrite-26.11.20260804.85f6261` (log: `logs/nix-build-pyrite-toplevel-postfix-*.log`; an earlier pre-correction build also succeeded, `logs/nix-build-pyrite-toplevel-20260908-214912.log`). The `/etc/dconf` databases cited under 1.1 and 1.2 were read out of that built system. The caveat in this task stands: this proves the flake's database content only, not what `gsd-power` resolves at runtime once the user-db sits ahead of it.
- [x] 2.2 Deploy — verify: `clan machines update pyrite 2>&1 | tee logs/clan-update-pyrite-$(date +%Y%m%d-%H%M%S).log` completes and the machine is still reachable afterwards. Deploying to pyrite carries the standing hazard that `wlp2s0` is lost across a warm reboot (`retrospective.md:233-234`); do not reboot as part of this task.
  - Evidence: the deploy ran at ~02:24 UTC on 2026-09-09 and is logged at `logs/pyrite-20260908-222347.log` (the log filename does not follow the `logs/clan-update-pyrite-*` pattern the verify line suggests, but its contents are unambiguously that command's output). `:50-57` show the built toplevel `/nix/store/br1zazj6bp3ksa7r84ngzhi3c42xhbiw-nixos-system-pyrite-26.11.20260804.85f6261` being set as the system profile via `nix-env -p /nix/var/nix/profiles/system --set`, then `switch-to-configuration boot` and `switch`. On the machine, `readlink -f /run/current-system` is exactly `br1zazj…`. The machine is reachable over `ssh pyrite.zt` and `systemctl --failed` lists `0 loaded units`. No reboot was performed. Post-deploy confirmation: `logs/pyrite-never-sleep-postdeploy.md` §4.1, §4.2.
  - Note: this box was checked by the post-deploy verification pass, which did not itself run `clan machines update`; it confirmed the deploy landed and is live rather than performing it.
- [x] 2.3 After the deploy in 2.2, restart the greeter — `systemctl restart display-manager` on pyrite. This is required, not optional: `switch-to-configuration switch` deliberately does not restart `display-manager.service`, which is listed under "NOT restarting the following changed units" in `logs/pyrite-20260802-121637.log:4968`. Until this runs, the greeter keeps running with its **old** dconf profile and will still suspend at the 900 s schema default, so task 4.1 cannot pass. **This terminates the active graphical session**: any logged-in desktop session is killed and unsaved work is lost, so run it from ssh with the machine's user warned, or run it while the machine sits at the greeter — verify: `systemctl show -p ActiveEnterTimestamp display-manager` reports a timestamp later than the `switch-to-configuration` run, and the greeter's `gsd-power` process has been re-executed under the new profile.
  - Evidence: `systemctl show -p ActiveEnterTimestamp -p ExecMainStartTimestamp display-manager.service` → both `Wed 2026-09-09 02:25:13 UTC`. That is 62 minutes after the 01:22:53 boot and ~1 minute after the ~02:24 activation, so it is a genuine post-activation restart and not the boot-time instance.
  - The greeter's `gsd-power` was re-executed under the new profile: `ps` shows PID 17259, uid 60578 (`gdm-greeter`), unit `user@60578.service`, started `Wed Sep 9 02:25:14 2026`, binary `/nix/store/kk6d5smf1x0nhyvw463g8x92w41rg56a-gnome-settings-daemon-50.1/libexec/gsd-power`; `sudo cat /proc/17259/environ` gives `DCONF_PROFILE=gdm`.
  - That it is reading the **new** profile is proved by comparing generations rather than by the unit's store path (the unit file itself is unchanged by this deploy — `autoSuspend` changes the dconf profile, not the unit): `cat /run/booted-system/etc/dconf/profile/gdm` lists two databases (`user-db:user`, `file-db:…3jlw9…-check-dconf-db`), while `/etc/dconf/profile/gdm` — identical to `/run/current-system`'s — lists three, with `file-db:/nix/store/r4clhhgiag7ndgn7vaka42sqisy0hb5p-dconf-db` inserted ahead of gdm's own defaults. Detail: `logs/pyrite-never-sleep-postdeploy.md` §1.1, §4.1.

## 3. Clearing the stale local override on the machine

- [x] 3.1 As cameron on pyrite, reset the two keys GNOME Settings wrote into the user database on 2026-09-08 (`logs/pyrite-graphical-session-idle-evidence.md` §5) so the flake's database becomes the operative one: `dconf reset /org/gnome/settings-daemon/plugins/power/sleep-inactive-ac-timeout` and `dconf reset /org/gnome/settings-daemon/plugins/power/sleep-inactive-ac-type` — verify: `DCONF_PROFILE` pointing at a throwaway `user-db:user`-only profile dumps a database with no `org/gnome/settings-daemon/plugins/power` stanza, per the isolation technique in `logs/pyrite-idle-suspend-diagnosis.md` Appendix. This is the only step in this change that is a state change on the machine rather than a configuration edit.
  - Evidence (before): `DCONF_PROFILE=/tmp/vxprof/only-user dconf dump /org/gnome/settings-daemon/plugins/power/` → `sleep-inactive-ac-timeout=3600`, `sleep-inactive-ac-type='nothing'` — exactly the two keys the Power panel wrote, and no others (`dconf list` on the isolated user db showed no battery keys). `stat` → mtime `2026-09-08 22:33:44`, 3962 bytes, unchanged by the deploy. They were still winning: `dconf read …/sleep-inactive-ac-timeout` → `3600`.
  - Safety: the reset was performed **through the normal writer**, not forced. Although cameron's graphical session was terminated by the 2.3 restart (`loginctl show-session 4` → `Active=no State=closing`), a `dconf` write needs the user *session bus*, not a graphical session, and cameron has `LINGER=yes`, so `user@1000.service` is active, `/run/user/1000/bus` exists, and `ca.desrt.dconf` is D-Bus-activatable on it. No uid-1000 `gsd-power` is running to race the write. A byte-for-byte backup was taken first (`/tmp/vxprof/user-db-backup-before-reset`, sha256 `b9831f45…d626c7`).
  - Evidence (after): the two `dconf reset` commands above were run — and only those two; no `reset -f /`, nothing outside `org/gnome/settings-daemon/plugins/power/`. `DCONF_PROFILE=/tmp/vxprof/only-user dconf dump /org/gnome/settings-daemon/plugins/power/` now returns **empty**, and `dconf list /org/gnome/settings-daemon/plugins/` returns only `color/` and `housekeeping/` — no `power/`. `stat` → `2026-09-09 02:29:30`, 3772 bytes.
  - Nothing else in the user db moved: a full before/after dump diff (before rendered from the backup via `XDG_CONFIG_HOME`) is exactly four removed lines — the `[org/gnome/settings-daemon/plugins/power]` header plus the two keys plus the blank line — with zero additions and zero other hunks.
  - Effective values now come from the Nix file-db: `dconf dump /org/gnome/settings-daemon/plugins/power/` → `0 / 'nothing' / 0 / 'nothing'`, `dconf read /org/gnome/desktop/session/idle-delay` → `uint32 1800`. Since the user db holds none of these keys, all five resolve from `file-db:/nix/store/xffqrfxrqgs2p8h8bqsf6cqsbzzc72y3-dconf-db`. Full detail: `logs/pyrite-never-sleep-postdeploy.md` §3.
- [x] 3.2 Read the effective values the way `gsd-power` reads them, not the way a plain ssh shell does — verify: with `XDG_DATA_DIRS`, `XDG_RUNTIME_DIR`, and `DBUS_SESSION_BUS_ADDRESS` lifted from the running `gsd-power`'s `/proc/<pid>/environ` (the procedure in `logs/pyrite-graphical-session-idle-evidence.md` §3.2, needed because the gsd schemas are not on a plain ssh session's `XDG_DATA_DIRS`), `gsettings get org.gnome.settings-daemon.plugins.power` reports `sleep-inactive-ac-timeout 0`, `sleep-inactive-battery-timeout 0`, `sleep-inactive-ac-type 'nothing'`, `sleep-inactive-battery-type 'nothing'`, and `org.gnome.desktop.session idle-delay` still reports `uint32 1800`.
  - **Discharge method: runtime observation, exactly as specified.** Closed at 03:23 UTC on 2026-09-09, after the cold boot at 03:18:21 and the graphical login at 03:19. A uid-1000 `gsd-power` now exists — PID 2200, user `cameron`, unit `user@1000.service`, started `Wed Sep 9 03:19:00 2026`, binary `/nix/store/kk6d5smf1x0nhyvw463g8x92w41rg56a-gnome-settings-daemon-50.1/libexec/gsd-power` — which is what the earlier pass lacked.
  - `XDG_DATA_DIRS`, `XDG_RUNTIME_DIR` (`/run/user/1000`) and `DBUS_SESSION_BUS_ADDRESS` (`unix:path=/run/user/1000/bus`) were lifted verbatim from `/proc/2200/environ` and exported before the reads. `gsettings` then reported:

    ```
    sleep-inactive-ac-timeout        = 0
    sleep-inactive-ac-type           = 'nothing'
    sleep-inactive-battery-timeout   = 0
    sleep-inactive-battery-type      = 'nothing'
    session idle-delay               = uint32 1800
    ```

    All five expected values, resolved through the schema, in the daemon's own environment.
  - Honest detail: `/proc/2200/environ` carries **no** `DCONF_PROFILE`, so this daemon uses dconf's default profile name `user` — i.e. `/etc/dconf/profile/user`, which is `user-db:user` then `file-db:/nix/store/xffqrfxrqgs2p8h8bqsf6cqsbzzc72y3-dconf-db`. Since cameron's user db still carries no `org/gnome/settings-daemon/plugins/power` stanza after the cold boot and the new login (isolated `user-db:user`-only dump is empty; `stat` still `2026-09-09 02:29:30`, 3772 bytes — untouched by the boot), every one of these five values resolves from the Nix file-db. This is the daemon's own environment; it is still a `gsettings` client read rather than an introspection of the daemon's internal state, which no supported interface exposes.

## 4. Integration verification

- [x] 4.1 Verify the login screen no longer suspends — verify: leave pyrite at the greeter, on AC, untouched for at least 25 minutes with an ssh session watching, then `journalctl -b | grep -c "suspend requested"` returns `0`. Twenty-five minutes exceeds both the 900 s greeter default that fired twice at 15 m 00 s and 15 m 01 s in `logs/pyrite-idle-suspend-diagnosis.md` §1.2 and any residual margin.
  - **Discharge method: wall-clock behavioural observation. Passed, by a margin of roughly 3× the failing threshold.** The machine sat at the greeter (session `c3`, `gdm-greeter`, class `greeter`, wayland) from the display-manager restart at **02:25:13 UTC** until the operator's graphical login at **03:16:09 UTC** — **50 m 56 s**. The operator's own conservative anchor, an ssh probe at 03:11:01 UTC, gives a verified floor of **45 m 48 s**. Either figure is far past the 900 s at which the old failure mode fired.
  - Counts across that window, re-derived here from `boot -1` (`aaca9706…`) rather than taken on report: `journalctl -b -1 --since "2026-09-09 02:25:13" --until "2026-09-09 03:11:01" | grep -ciE "suspend requested|PM: suspend entry"` → **0**. Whole-boot `PM: suspend entry` count → **1**, and whole-boot `suspend requested` count → **1**; both are the single 03:16:19–03:16:21 manual suspend of task 4.4, i.e. **zero idle-initiated suspends**, which is the assertion this task exists to make. The literal `grep -c` in the verify line returns `1` for the boot as a whole, and it would be dishonest to report `0` without saying why: the one hit is `systemd-logind[988]: suspend requested from client PID 22217 ('.gnome-session-')`, a person choosing to suspend, not an idle watch firing.
  - Continuity: no `PM: suspend entry` and no journal gap between 02:25:13 and 03:16:09, so the machine was awake for the whole interval rather than having suspended and resumed unobserved.
  - Not a dead-daemon artefact: the greeter's `gsd-power` (PID 17259, uid 60578, `DCONF_PROFILE=gdm`, started 02:25:14) was alive throughout the window and is the process that would have armed the watch. The database it reads was confirmed under 1.1/2.3 to hold `0 / 'nothing' / 0 / 'nothing'`.
- [x] 4.2 Verify the logged-in session no longer suspends on battery — verify: log in at the panel, unplug the charger, leave it untouched for at least 35 minutes, then confirm `journalctl -b | grep -c "suspend requested"` is still `0`. Thirty-five minutes exceeds the 1800 s battery timeout that was previously declared. This is the case no prior report observed directly, and it is the one that would fail if the `-type` keys had not been set.
  - **Discharge method: evaluation, not observation — by explicit operator decision.** The charger was never unplugged and no 35-minute battery window was watched. The operator elected to accept configuration evidence for the battery path rather than spend the wall-clock time. This box is checked on that decision, and the sentence in the verify line "35 minutes exceeds the 1800 s battery timeout" was **not** executed. Recording it any other way would misrepresent what happened.
  - The configuration evidence, decoded **on the machine** rather than in the store, and re-confirmed after the 03:18:21 cold boot: the greeter file-db `/nix/store/r4clhhgiag7ndgn7vaka42sqisy0hb5p-dconf-db` and the user file-db `/nix/store/xffqrfxrqgs2p8h8bqsf6cqsbzzc72y3-dconf-db` both dump `sleep-inactive-battery-timeout=0` and `sleep-inactive-battery-type='nothing'`, and the live `gsd-power` (PID 2200) resolves both through the schema (task 3.2).
  - Why the evaluation is load-bearing rather than hand-waving: per gnome-settings-daemon 50.1 `plugins/power/gsd-power-manager.c` `idle_configure()` — `if (timeout_sleep != 0) { if (action_type != GSD_POWER_ACTION_NOTHING) { … add_idle_watch … } }`, with both watch ids cleared unconditionally beforehand at `:2082-2085` — **either** key alone suppresses registration of the idle sleep watch, and both are set here. The AC and battery paths differ only in which pair of keys `idle_configure()` reads for the current power source; they share one code path and one watch.
  - What 4.1 adds to this: the 50-minute AC observation confirms that mechanism end-to-end on this machine — keys → `idle_configure()` → no watch → no suspend. The residual risk this box does not retire is that the battery branch reads a different key pair, which is a source-level claim verified by reading `idle_configure()` and not by watching the machine on battery.
  - Current power state, for the record: `ADP1/online` → `1`, `BAT0/status` → `Full`. The machine has been on mains throughout.
- [x] 4.3 Verify the screen still blanks and locks — verify: during 4.2, the panel blanks at about 30 minutes and `loginctl show-session <id>` reports `LockedHint=yes`, confirming `idle-delay` and the screensaver defaults are untouched.
  - **Discharge method: evaluation, not observation — by explicit operator decision, and the weaker of the two evaluation discharges.** Nobody watched the panel and no `LockedHint=yes` was read after a 30-minute idle interval. Checked on the operator's decision to accept configuration evidence.
  - Evidence: `idle-delay` is present and unchanged at `uint32 1800` in the compiled user file-db decoded on the machine, and the live `gsd-power` (PID 2200) resolves `org.gnome.desktop.session idle-delay = uint32 1800` (task 3.2). Cameron's user db carries no override for it. The change adds no key under `org/gnome/desktop/screensaver` and removes none, and task 1.4 shows the diff touches only the four power keys plus comments, so the screensaver defaults are untouched by construction.
  - Stated plainly, because the gap is real: "`idle-delay` is 1800 and nothing in the diff touches the screensaver" entails that this change did not *break* blanking and locking. It does not constitute an observation that the panel blanks and the session locks. If that observation is wanted, it is one 30-minute idle window away and needs no redeployment. Carried into `verify.md` §7 as a residual coverage gap.
- [x] 4.4 Verify manual suspend still works — verify: with the operator physically present and accepting the resume risk this change does not remove, `systemctl suspend` suspends the machine, `journalctl -b -1 | grep "Performing sleep operation"` on the next boot shows the operation was attempted, and `systemctl status disable-d3cold-all nvme-d3cold-suspend-guard` shows both ran on that transition. A failed resume here is the known defect and is not a regression from this change; record the outcome either way, since each attempt is a datapoint `logs/pyrite-resume-failure-diagnosis.md` §4.5 item 1 asks for.
  - **Discharge method: behavioural observation, operator-triggered. The three assertions this verify line makes were all met. The machine then failed to resume, which this verify line pre-declares as the known defect rather than a failure of this task — but the box is checked on the assertions as written, not on any claim that a usable wake was observed.**
  - What was invoked: the operator chose *Suspend* from the GNOME menu at 03:16:19 UTC. Journal: `systemd-logind[988]: suspend requested from client PID 22217 ('.gnome-session-') (unit user@1000.service)`. That is the same logind `Suspend()` path `systemctl suspend` takes, reached from the desktop instead of a shell; the verify line names `systemctl suspend` and this was the menu, and the difference is the caller, not the path.
  - The invocation half — **discharged affirmatively**, all three assertions verified here from `boot -1` and not taken on report:

    ```
    03:16:21  Starting Disable PCIe d3cold on all devices before sleep...
    03:16:21  disable-d3cold-all.service: Deactivated successfully.
    03:16:21  Finished Disable PCIe d3cold on all devices before sleep.
    03:16:21  Starting Refuse suspend unless NVMe d3cold is disabled...
    03:16:21  nvme-d3cold-suspend-guard.service: Deactivated successfully.
    03:16:21  Finished Refuse suspend unless NVMe d3cold is disabled.
    03:16:21  Starting System Suspend...
    03:16:21  systemd-sleep[22917]: Successfully froze unit 'user.slice'.
    03:16:21  kernel: PM: suspend entry (deep)
    03:16:21  systemd-sleep[22917]: Performing sleep operation 'suspend'...
    ```

    `Performing sleep operation` is present, and it is the **last line of boot −1** — the journal ends there. Both guard units ran and both succeeded (`Deactivated successfully` is a oneshot's normal exit, and `Result=success`). Kernel `6.18.42`.
  - The resume half — **failed. CAM-59 reproduced.** No line follows `Performing sleep operation` in boot −1. Boot `0` (`55dc8d7c…`) begins at **03:18:21 UTC**, from the operator's power cycle. Dark interval **2 m 00 s**, so this is a wedged resume and not a long one that was cut short prematurely; the operator was present and cycled power because the machine did not come back.
  - **This does not reflect on this change.** Nothing in it touches the suspend path: task 1.4 shows the diff is four dconf-key lines plus comments, with no hunk on `services.logind.settings.Login`, `disable-d3cold-all`, `nvme-d3cold-suspend-guard`, `disable-lid-wakeup`, or `boot.kernelParams`. The defect predates the change by 7 recorded failures against 30 successes across 14 boots (`logs/pyrite-resume-failure-diagnosis.md` §1.3), is tracked separately as CAM-59, and is named as a non-goal in `proposal.md` and as world assumption A13 in this change's `world-assumptions` delta. A failed resume **confirms** A13; it does not violate it, and A13's stated violation condition is the opposite observation.
  - Datapoint for `logs/pyrite-resume-failure-diagnosis.md` §4.5 item 1, in that file's terms: boot `aaca9706…`, kernel 6.18.42, **first** suspend of the boot, initiated from the desktop menu at 03:16:19, entered `deep` at 03:16:21, never returned, power-cycled at ~03:18:21 after a 2 m 00 s dark interval, operator present. Notable against §5.1's generation-boundary correlation and against the "the first suspend of a boot never fails (7/7)" asymmetry in §4.4 — this one **was** the first suspend of its boot and it failed, which is a counterexample to that asymmetry and should be carried into that file when it is next revised.
  - What this box does **not** establish: that a suspend on this machine can be woken from usefully. It never could, reliably, and this change never claimed to change that.
- [x] 4.5 Confirm the declined and out-of-scope items are still where they were — verify: `busctl get-property org.freedesktop.login1 /org/freedesktop/login1 org.freedesktop.login1.Manager HandleLidSwitch` still reports `lock`; `systemctl is-enabled sleep.target` reports it unmasked; and `for d in /sys/bus/acpi/devices/*/power/wakeup; do ...; done` still shows `PNP0C0D:00` disabled, i.e. this change did not quietly re-enable the lid as a wake source.
  - Evidence, all read on the deployed machine at 02:29–02:30 UTC on 2026-09-09: `busctl get-property … HandleLidSwitch` → `s "lock"`. `systemctl list-unit-files sleep.target suspend.target hibernate.target` → all three `linked` (NixOS's normal state; `/etc/systemd/system/sleep.target -> /nix/store/r6sz8p6sd6c73fp9z8nzl04dri7lyx8n-systemd-261.1/example/systemd/system/sleep.target`) — **not masked**. ACPI wakeup: `PNP0C0D:00 disabled` (lid), `PNP0C0C:00 enabled` (power button), i.e. the power button remains the sole wake source and the lid was not re-armed.
  - Also confirmed while here, since the change must not have disturbed them: `IdleAction` → `s "ignore"` (`IdleActionUSec` `t 1800000000`, unused while the action is ignore); `disable-d3cold-all`, `nvme-d3cold-suspend-guard` and `disable-lid-wakeup` all present with `UnitFileState=enabled` and `Result=success`, wiring intact (`WantedBy=systemd-suspend.service …`, `RequiredBy=systemd-suspend.service …`, `WantedBy=multi-user.target` respectively; the first two correctly inactive between sleep transitions, the third active/exited). `systemctl --failed` → `0 loaded units listed`.
  - Manual suspend remains possible and was **not** invoked: `sudo busctl call … CanSuspend` → `s "yes"` (as cameron over ssh it is `s "challenge"`, the ordinary polkit answer for a non-active remote session, meaning permitted-subject-to-authentication, not refused). `SleepOperation` advertises `["suspend-then-hibernate", "suspend", "hibernate"]`. `CanHibernate` → `"na"`, pre-existing and unrelated. Detail: `logs/pyrite-never-sleep-postdeploy.md` §4.5, §4.6.
  - **Re-confirmed after the cold boot** of 03:18:21 UTC (a fresh test, since the earlier reads were on a warm activation): `HandleLidSwitch` → `s "lock"`, `IdleAction` → `s "ignore"`, `PNP0C0D:00 disabled` / `PNP0C0C:00 enabled`, and all three guard units still `enabled`. Nothing was re-armed by the reboot. Full post-power-cycle health record in `verify.md` §9.
