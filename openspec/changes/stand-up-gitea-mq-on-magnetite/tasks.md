## 1. Forge application

- [ ] 1.1 [G1 — operator gate; an agent MUST NOT tick this box] Present the registration for the queue's GitHub App and stop: the helper URL `https://mic92.github.io/gitea-mq/` or the manual form at `https://github.com/organizations/sciexp/settings/apps/new`; proposed name `sciexp-gitea-mq`, owner `sciexp`, public; homepage `https://mq.scientistexperience.net/`; webhook URL `https://mq.scientistexperience.net/webhook/github` and webhook secret left blank, because the service patches both at startup; repository permissions Contents read and write, Administration read and write, Checks read and write, Pull requests read and write, Commit statuses read, Metadata read; subscribed events `pull_request`, `check_run`, `status`, `installation`, `installation_repositories`; then the exact credential commands the operator runs after generating a private key on the App's settings page — `clan vars set magnetite gitea-mq-github-app-secret-key/key.pem` pasting the PEM at the prompt, and for the webhook secret either nothing (`clan vars generate magnetite --generator gitea-mq-github-webhook-secret` in task 3.2 generates it and the service pushes it to the App) or `clan vars set magnetite gitea-mq-github-webhook-secret/secret` if the operator prefers to choose it — and wait for the operator to report the numeric App id — verify: the task text presented to the operator carries every value above verbatim, and the session records that it stopped here rather than proceeding
- [ ] 1.2 After the operator reports the App id, confirm the registration through `gh api /apps/<slug>` and record id, slug, owner, and the permission and event sets in this change's verify.md — verify: the API's `permissions` object is exactly `{administration: write, checks: write, contents: write, metadata: read, pull_requests: write, statuses: read}` and its `events` array is exactly the five events, and the id differs from `4743700`
- [ ] 1.3 Install the App on `cameronraysmith/vanixiets` alone and record the selection verbatim — verify: `gh api /repos/cameronraysmith/vanixiets/installation` under the App's identity, or the App's installations page, shows exactly that repository, recorded in verify.md
- [ ] 1.4 Confirm `sciexp-nixbot` is unchanged — verify: `gh api /apps/sciexp-nixbot` reports permissions `{checks: write, contents: read, members: read, metadata: read, pull_requests: read}` and events `[check_run, check_suite, pull_request, push]`, matching verify.md §9.1 of `stand-up-nixbot-on-magnetite`

## 2. Flake input

- [ ] 2.1 Add the `gitea-mq` flake input to `flake.nix` after the `nixbot` block, with `inputs.nixpkgs.follows = "nixpkgs"` and `inputs.treefmt-nix.follows = "treefmt-nix"`, and lock it — verify: `nix flake metadata --json | jq -r '.locks.nodes["gitea-mq"].locked.rev'` prints a revision, and `git diff HEAD -- flake.lock` adds only the `gitea-mq` node
- [x] 2.2 Confirm the existing `nixbot` and `buildbot-nix` inputs are untouched — verify: `nix flake metadata --json | jq -r '.locks.nodes.nixbot.locked.rev, .locks.nodes["buildbot-nix"].locked.rev'` prints the same two revisions as `git show HEAD:flake.lock` did

## 3. Credentials

- [ ] 3.1 Declare two `clan.core.vars` generators in the new aspect — `gitea-mq-github-app-secret-key` with file `key.pem` whose script exits non-zero with a "populate via clan vars set" message, and `gitea-mq-github-webhook-secret` with file `secret` generated by `openssl rand -hex 32` — each file at the default owner `root`, each naming `gitea-mq.service` in `restartUnits` — verify: `nix eval .#nixosConfigurations.magnetite.config.clan.core.vars.generators --apply 'g: { key = g.gitea-mq-github-app-secret-key.files."key.pem"; hook = g.gitea-mq-github-webhook-secret.files.secret; }' --json` shows `owner` `root` and `restartUnits` `["gitea-mq.service"]` on both, and no `owner = "gitea-mq"` anywhere in the aspect
- [ ] 3.2 Generate the webhook secret and populate the private-key slot with the value from task 1.1 — verify: `clan vars list magnetite` reports both generators as set, and `vars/per-machine/magnetite/` carries `gitea-mq-github-app-secret-key/key.pem/secret` and `gitea-mq-github-webhook-secret/secret/secret`, each a sops envelope
- [ ] 3.3 Confirm no credential value entered the repository — verify: a positive-controlled content search for an interior line of the PEM and for the whole webhook secret returns zero across the working tree including ignored paths, the tracked tree at HEAD, and `git log --all -S`, while the control string `mq.scientistexperience.net` returns non-zero in each layer; counts only, no value printed, following `stand-up-nixbot-on-magnetite` verify.md §11

## 4. First-party aspect

- [ ] 4.1 Write `modules/nixos/gitea-mq.nix` defining `flake.modules.nixos.gitea-mq` with `services.gitea-mq` enabled, `github.appId` from task 1.2, `github.privateKeyFile` and `github.webhookSecretFile` from the two generators, `github.repos = [ "cameronraysmith/vanixiets" ]`, `externalUrl = "https://mq.scientistexperience.net"`, `listenAddr = "127.0.0.1:8092"`, `hideRefFromClients = false`, `databaseUrl` at its default, `services.postgresql.ensureDatabases = [ "gitea-mq" ]` and `ensureUsers = [ { name = "gitea-mq"; ensureDBOwnership = true; } ]`, and `services.nginx.virtualHosts."mq.scientistexperience.net"` with `forceSSL`, `enableACME`, and `locations."/".proxyPass = "http://127.0.0.1:8092"` — verify: `nix eval .#nixosConfigurations.magnetite.config.services.gitea-mq --apply 's: { inherit (s) externalUrl listenAddr hideRefFromClients databaseUrl; repos = s.github.repos; appId = s.github.appId; }' --json` returns those values, `nix eval .#nixosConfigurations.magnetite.config.services.postgresql.ensureDatabases --json` contains `gitea-mq`, and `nix eval '.#nixosConfigurations.magnetite.config.services.nginx.virtualHosts."mq.scientistexperience.net".locations."/".proxyPass'` returns the loopback address
- [ ] 4.2 Set the four landing settings — `batchMax = 0`, `skipQueueIfUpToDate = true`, `requiredChecks = [ "nixbot/nix-eval" "nixbot/nix-build" ]`, and no assignment to the merge label anywhere — verify: `nix eval .#nixosConfigurations.magnetite.config.systemd.services.gitea-mq.environment --apply 'e: { inherit (e) GITEA_MQ_BATCH_MAX GITEA_MQ_SKIP_QUEUE_IF_UP_TO_DATE GITEA_MQ_REQUIRED_CHECKS; label = e ? GITEA_MQ_MERGE_LABEL; }' --json` returns `{"GITEA_MQ_BATCH_MAX":"0","GITEA_MQ_SKIP_QUEUE_IF_UP_TO_DATE":"true","GITEA_MQ_REQUIRED_CHECKS":"nixbot/nix-eval,nixbot/nix-build","label":false}`, and `rg -n GITEA_MQ_MERGE_LABEL modules/` finds only the assertion
- [x] 4.3 Add `assertions` to the aspect that read `config.services.gitea-mq.batchMax == 0`, `.skipQueueIfUpToDate == true`, `.requiredChecks == [ "nixbot/nix-eval" "nixbot/nix-build" ]`, and `!(config.systemd.services.gitea-mq.environment ? GITEA_MQ_MERGE_LABEL)`, each with a message naming the setting and the ADR — verify: `nix eval .#checks.x86_64-linux.nixos-magnetite.drvPath` succeeds as-is; then, as a negative control performed in a scratch edit and reverted, adding `services.gitea-mq.batchMax = lib.mkForce 1;` to the host makes the same command fail with the assertion's message, and adding `systemd.services.gitea-mq.environment.GITEA_MQ_MERGE_LABEL = "x";` fails with the label assertion's message; both failures captured in `logs/gitea-mq-assertion-negative-$(date +%Y%m%d-%H%M%S).log`
- [ ] 4.4 Give the aspect file a header documenting its two generators, the peer-authentication coupling between the unit name and the role name, the loopback port and why 8080 is not used, `hideRefFromClients` and why, and the ruleset precondition, following `modules/nixos/nixbot.nix`'s header form — verify: the header names each item, and `just lint` passes inside the dev shell

## 5. Host composition

- [ ] 5.1 Import `inputs.gitea-mq.nixosModules.default` in `modules/machines/nixos/magnetite/default.nix` after `inputs.nixbot.nixosModules.nixbot`, and name `gitea-mq` in the host's aspect list after `nixbot` — verify: `nix eval .#nixosConfigurations.magnetite.config.services.gitea-mq.enable` returns `true`
- [x] 5.2 Confirm the build services' composition is unchanged — verify: `git diff --stat` reports no change under `modules/nixos/nixbot.nix` or `modules/nixos/buildbot.nix`, and `nix eval .#nixosConfigurations.magnetite.config.services.nixbot.domain` still returns `nixbot.scientistexperience.net`
- [x] 5.3 Confirm the queue claims disjoint host resources in the evaluated configuration — verify: controller-run `nix eval` of the candidate magnetite configuration shows `systemd.services.gitea-mq.serviceConfig.DynamicUser` true, `CacheDirectory` `gitea-mq`, no `users.users.gitea-mq`, and the loopback listener at 8092; compare the complete evaluated `systemd.services.gitea.serviceConfig.ExecStartPre` against an independently identified immutable pre-S1 source, using the same projection `c: { pre = c.systemd.services.gitea.serviceConfig.ExecStartPre or null; }` with `nix eval --no-write-lock-file --json` for both sources. Record the baseline's full revision, evidence establishing its pre-S1 provenance, immutable source identity, and lock-file identity alongside the candidate's full revision, immutable source identity, and lock-file identity; retain both exact evaluation commands, exit statuses, complete results, and the comparison command, exit status, and result. Neither the already-adopted local candidate nor a candidate with queue settings disabled is a valid baseline. Keep this task unverified until provenance is established, both evaluations succeed, and the complete values compare equal without suppressing differences, establishing that `hideRefFromClients = false` injected nothing into the forge's unit. If baseline provenance cannot be established, block the `forge-pre-unchanged` observable; investigate mismatches rather than selecting a baseline because it produces equality.

## 6. Hostname

- [ ] 6.1 Add an unproxied `mq` CNAME to `modules/terranix/cloudflare.nix` following the `nixbot` record — verify: `just terraform-plan 2>&1 | tee logs/terraform-plan-mq-$(date +%Y%m%d-%H%M%S).log` reports exactly one record to add and none to change or destroy
- [ ] 6.2 Apply the plan and confirm public resolution — verify: `just terraform-apply` completes and `dig +noall +answer mq.scientistexperience.net` returns a CNAME to `magnetite.scientistexperience.net` and then the host's own A record `49.12.12.74`, not a provider anycast address

## 7. Build gate

- [x] 7.1 Instantiate the host's configuration without deploying it — verify: `nix eval .#checks.x86_64-linux.nixos-magnetite.drvPath 2>&1 | tee logs/nixos-magnetite-eval-gitea-mq-$(date +%Y%m%d-%H%M%S).log` succeeds, walking the whole host configuration including the new assertions; instantiation rather than realization, for the reason `stand-up-nixbot-on-magnetite` task 7.1 records (aarch64-darwin operator machine against an x86_64-linux target)

## 8. Rulesets

- [ ] 8.1 Read the current state of `cameronraysmith/vanixiets` and record it in verify.md — verify: `gh api /repos/cameronraysmith/vanixiets/rulesets`, `gh api /repos/cameronraysmith/vanixiets/rulesets/16212553`, `gh api /repos/cameronraysmith/vanixiets/branches/main/protection`, `gh api /repos/cameronraysmith/vanixiets --jq '{allow_auto_merge,allow_merge_commit,allow_squash_merge,allow_rebase_merge}'`, and `gh api /repos/cameronraysmith/vanixiets/collaborators --jq '.[].login'` are captured verbatim
- [ ] 8.2 [G2 — operator gate; an agent MUST NOT tick this box] Present the per-repository ruleset diff for approval and stop: before, ruleset `16212553` `nixbot` on `~DEFAULT_BRANCH` with rules `deletion`, `non_fast_forward`, `required_status_checks [nixbot/nix-build @ 4743700]`, bypass `RepositoryRole 5`; after, one ruleset named `gitea-mq` on `~DEFAULT_BRANCH` with rules `deletion`, `non_fast_forward`, `required_linear_history`, `required_status_checks [gitea-mq @ <App id from task 1.2>]` with `strict_required_status_checks_policy false` and `do_not_enforce_on_create true`, bypass actors `Integration <App id>` always, `User cameronraysmith` always, and `RepositoryRole 5` always, no `pull_request` rule, no `workflows` rule; classic protection on `main` unchanged; `allow_auto_merge` left `true`; and the reverse edit for rollback; then wait for the operator's approval or amendment — verify: the diff presented carries both states in full and the reverse edit, and the session records that it stopped here
- [ ] 8.3 After approval, apply the diff through `gh api --method PUT /repos/cameronraysmith/vanixiets/rulesets/16212553` (renaming and editing the existing ruleset rather than creating a second one) and record the result — verify: `gh api /repos/cameronraysmith/vanixiets/rulesets` lists exactly one branch ruleset named `gitea-mq`, and its `rules[] | select(.type == "required_status_checks")` names only `gitea-mq` pinned to the new App's id
- [ ] 8.4 Record the collaborator set that discharges the sole-enabler requirement — verify: `gh api /repos/cameronraysmith/vanixiets/collaborators --jq '.[].login'` lists `cameronraysmith` alone, and the two App installations (`sciexp-nixbot`, the queue's App) are the only other write-capable identities, recorded in verify.md

## 9. Deployment

- [ ] 9.1 Deploy the host — verify: `clan machines update magnetite 2>&1 | tee logs/clan-update-magnetite-gitea-mq-$(date +%Y%m%d-%H%M%S).log` completes, and on the host `systemctl is-active gitea-mq.service` reports `active`, `journalctl -u gitea-mq.service` shows the schema migrations applied over `/run/postgresql` with no authentication error, and `acme-mq.scientistexperience.net.service` has run

## 10. Documentation

- [ ] 10.1 Extend `packages/docs/src/content/docs/concepts/build-service-topology.md`, or a sibling note it links, with the queue's place beside the two build services: its hostname, its App, the four landing settings and the assertion that pins them, the ruleset shape, and the auto-merge disposition — verify: `just docs-build` succeeds and the documentation link check passes

## 11. Integration Verification

- [ ] 11.1 Verify the queue answers at its own hostname over an accepted connection and the build service still does — verify: `curl -o /dev/null -w '%{http_code} ssl_verify=%{ssl_verify_result}\n' https://mq.scientistexperience.net/` returns `200 ssl_verify=0` with a certificate issued for that hostname, and the same request to `https://nixbot.scientistexperience.net/` returns `200 ssl_verify=0`
- [ ] 11.2 Verify the service registered its own webhook endpoint and that deliveries are authenticated both ways — verify: `gh api /app/hook/config` under the App's JWT, or the App's settings page, shows URL `https://mq.scientistexperience.net/webhook/github`; a redelivery from the App's advanced settings returns `2xx` and appears in `journalctl -u gitea-mq.service`; and an unsigned `curl -X POST https://mq.scientistexperience.net/webhook/github -d '{}'` returns `401` or `403`, with both status codes recorded in verify.md
- [ ] 11.3 Verify the database and role exist for the dynamic user — verify: on the host `sudo -u postgres psql -c '\l'` lists `gitea-mq` owned by `gitea-mq`, `psql -c '\du'` shows the role without superuser or createdb, and `systemctl show gitea-mq.service -p User -p DynamicUser` reports `User=gitea-mq DynamicUser=yes`
- [ ] 11.4 Verify the queue's startup setup left the rulesets as approved — verify: `gh api /repos/cameronraysmith/vanixiets/rulesets` lists exactly one branch ruleset, named `gitea-mq`, and `journalctl -u gitea-mq.service` shows no ruleset creation and no "cannot enable allow_auto_merge" warning; `gh api /repos/cameronraysmith/vanixiets --jq .allow_auto_merge` is `true`
- [ ] 11.5 Verify V3 and the resolved required-check list at runtime — verify: on a pull request whose tip nixbot has built, `gh api /repos/cameronraysmith/vanixiets/commits/<sha>/check-runs --jq '.check_runs[].name'` lists `nixbot/nix-eval` and `nixbot/nix-build` exactly, and `journalctl -u gitea-mq.service` at debug level, or the dashboard's per-repository view, shows the queue resolving required checks to that pair rather than to a forge-supplied list
- [ ] 11.6 Verify the four landing settings in the running unit's environment — verify: `systemctl show gitea-mq.service -p Environment` on the host carries `GITEA_MQ_BATCH_MAX=0`, `GITEA_MQ_SKIP_QUEUE_IF_UP_TO_DATE=true`, `GITEA_MQ_REQUIRED_CHECKS=nixbot/nix-eval,nixbot/nix-build`, and no `GITEA_MQ_MERGE_LABEL`
- [ ] 11.7 Verify V2 and the single-entry fast-forward path end to end — verify: on a single pull request whose head contains `main`'s tip and whose head SHA already carries both nixbot check runs as passed before the label is applied, applying the `merge-queue` label as `cameronraysmith` results in `main` advancing to that head SHA with no merge commit (`git log --first-parent -1 --format=%P origin/main` shows one parent), the pull request reading as merged with a `merged_at` timestamp, a `gitea-mq` status of success on the head, and no `gitea-mq/batch/*` branch created; the elapsed time from label to landing recorded in verify.md
- [ ] 11.8 Record the V6 observation — verify: a push of `refs/landings/v6-probe` from the orchestrator identity to `cameronraysmith/vanixiets` either succeeds or is refused, and the outcome is recorded with the note that rulesets target branches, tags, pushes, or the repository and therefore cannot protect that namespace from deletion; the probe ref is deleted afterward
- [ ] 11.9 Verify the rollback path is available — verify: removing `gitea-mq` from magnetite's aspect list in a scratch edit makes `nix eval .#checks.x86_64-linux.nixos-magnetite.drvPath` succeed with a different derivation path, the edit is reverted, and the ruleset's reverse edit from task 8.2 is recorded as the accompanying forge-side rollback
