## 1. Record the baseline and add the assertion pair

- [ ] 1.1 Run the transitive probe procedure by hand at the starting revision on aarch64-darwin (`nix eval --json .#checks.aarch64-darwin --apply 'cs: builtins.mapAttrs (_: c: c.drvPath) cs'`, then `nix-store -q --requisites <drv> | grep -qx "$(nix flake metadata --json | jq -r .path)"` per check) and record the offender list in `logs/check-source-audit-baseline-<timestamp>.log` — verify: the log lists exactly `darwin-argentum darwin-blackphos darwin-rosegold darwin-stibnite gitleaks home-manager-cameron home-manager-christophersmith home-manager-crs58 home-manager-janettesmith home-manager-raquel home-manager-tara machine-registry-completeness secrets-encryption-integrity secrets-sops-roundtrip secrets-tier-separation treefmt vars-user-password-validation`, and `git check-ignore -q logs/` exits 0
- [ ] 1.2 Add `modules/checks/structure/check-source-isolation.nix` exposing `flake.lib.checkSourceAllowList` as a list of `{ name; reason; }` attrsets, a classifier `referencesSource = drv: builtins.elem (builtins.unsafeDiscardStringContext self.outPath) (builtins.attrNames (builtins.getContext (builtins.toJSON drv.drvAttrs)))`, and `checks.structure-check-source-isolation` built with `self.lib.mkStructuralCheck` whose `actual` is the sorted names of `self'.checks` (excluding the two structural attributes) for which the classifier is true and whose `expected` is the sorted allow-list names; seed the allow-list with the seven current direct offenders so the check is green at the baseline — verify: `nix eval --raw .#checks.aarch64-darwin.structure-check-source-isolation.drvPath` succeeds without `--impure`, and `nix build --option builders '' .#checks.aarch64-darwin.structure-check-source-isolation --no-link` exits 0
- [ ] 1.3 Add `checks.structure-check-source-isolation-neg` in the same module, applying the classifier to `pkgs.runCommand "fixture-self" { src = self; } "touch $out"` and `pkgs.runCommand "fixture-filtered" { src = lib.fileset.toSource { root = ../..; fileset = ../../flake.nix; }; } "touch $out"` with `expected = { self = true; filtered = false; }` — verify: `nix build --option builders '' .#checks.aarch64-darwin.structure-check-source-isolation-neg --no-link` exits 0, and temporarily changing the fixture-self `src` to the filtered source makes the same build fail with a diff naming `self`
- [ ] 1.4 Confirm the evaluation-time classifier matches the direct group of the baseline — verify: `nix eval --raw .#checks.aarch64-darwin.structure-check-source-isolation.actualJson` lists exactly `gitleaks machine-registry-completeness secrets-encryption-integrity secrets-sops-roundtrip secrets-tier-separation treefmt vars-user-password-validation`

## 2. Filter the five validation checks

- [ ] 2.1 In `modules/checks/validation.nix` add a local `srcOf = paths: lib.fileset.toSource { root = ../..; fileset = lib.fileset.unions paths; }` and rewrite `vars-user-password-validation` to read `${srcOf [ ../../vars ]}/vars/shared/user-password-cameron` — verify: `nix build --option builders '' .#checks.aarch64-darwin.vars-user-password-validation --no-link -L` exits 0 and prints the same `OK:` lines as the baseline build of the same check
- [ ] 2.2 Rewrite `secrets-tier-separation` and `secrets-encryption-integrity` to read `${srcOf [ ../../vars ../../secrets ]}/vars` and `/secrets`, removing the `SKIP` branches that guarded a missing `${self}` directory — verify: `nix build --option builders '' .#checks.aarch64-darwin.secrets-tier-separation .#checks.aarch64-darwin.secrets-encryption-integrity --no-link -L` exits 0 with the same `OK:` lines as baseline
- [ ] 2.3 Rewrite `machine-registry-completeness` to read `${srcOf [ ../../modules/machines/darwin ../../modules/machines/nixos ]}/modules/machines/{darwin,nixos}` and `secrets-sops-roundtrip` to test `${srcOf [ ../../.sops.yaml ]}/.sops.yaml` — verify: `nix build --option builders '' .#checks.aarch64-darwin.machine-registry-completeness .#checks.aarch64-darwin.secrets-sops-roundtrip --no-link -L` exits 0 with the same `OK:` lines as baseline
- [ ] 2.4 Shrink the allow-list to `gitleaks` and `treefmt` and rebuild the assertion pair — verify: `nix build --option builders '' .#checks.aarch64-darwin.structure-check-source-isolation .#checks.aarch64-darwin.structure-check-source-isolation-neg --no-link` exits 0, and `nix eval --raw .#checks.aarch64-darwin.secrets-tier-separation.drvPath` is unchanged after appending a line to `docs/notes/README.md` in the working copy and reverting it

## 3. Filter treefmt through its projectRoot option

- [ ] 3.1 In `modules/formatting.nix` set `treefmt.projectRoot = lib.fileset.toSource { root = ../.; fileset = lib.fileset.unions [ ../flake.nix (lib.fileset.fileFilter matchesAnyInclude ../.) ]; }`, where `matchesAnyInclude` translates each `config.treefmt.settings.formatter.<name>.includes` pattern of the shape `*.<ext>` into `file.hasExt "<ext>"` and calls `throw` naming any pattern of another shape — verify: `nix eval --json .#checks.aarch64-darwin.treefmt.drvPath` succeeds, `nix build --option builders '' .#checks.aarch64-darwin.treefmt --no-link -L` exits 0
- [ ] 3.2 Prove the translation guard fires — verify: with `treefmt.settings.formatter.nixfmt.includes = [ "nix/**" ]` temporarily set, `nix eval .#checks.aarch64-darwin.treefmt.drvPath` fails with an error naming `nix/**`; the setting is then reverted
- [ ] 3.3 Shrink the allow-list to `gitleaks` with the reason "credential scan must cover every tracked file" and rebuild the assertion pair — verify: `nix build --option builders '' .#checks.aarch64-darwin.structure-check-source-isolation .#checks.aarch64-darwin.structure-check-source-isolation-neg --no-link` exits 0 and `nix eval --json .#lib.checkSourceAllowList` prints one entry named `gitleaks`

## 4. Replace the module-level source coercions

- [ ] 4.1 In each of `modules/home/users/{christophersmith,crs58,janettesmith,raquel,tara,ubuntu}/default.nix` replace `flake.inputs.self + "/secrets/home-manager/users/<user>/secrets.yaml"` with the path literal `../../../../secrets/home-manager/users/<user>/secrets.yaml` — verify: `nix eval --raw '.#homeConfigurations."crs58@aarch64-darwin".config.sops.defaultSopsFile'` prints a store path ending in `-secrets.yaml`, and `nix build --option builders '' .#checks.aarch64-darwin.home-manager-crs58 --no-link` exits 0
- [ ] 4.2 In `modules/nixos/hm-sops-bridge.nix` replace `inputs.self + "/secrets/bridge/${userCfg.sopsIdentity}-age-key.enc"` with `../../secrets/bridge + "/${userCfg.sopsIdentity}-age-key.enc"` — verify: `nix eval --json .#nixosConfigurations.magnetite.config.sops.secrets --apply 'ss: map (s: s.sopsFile) (builtins.attrValues ss)'` prints the `crs58-age-key.enc` entry as a store path whose name is `-crs58-age-key.enc` rather than a path under `-source/secrets/bridge/`
- [ ] 4.3 In `modules/home/ai/openspec/default.nix` set `assetsDir = ./assets` and update the `defaultText` of `programs.openspec.package` to the literal-path form — verify: `nix eval --raw '.#homeConfigurations."crs58@aarch64-darwin".config.programs.openspec.package.superpowers-bridge-wrspm'` prints a store path ending in `-assets/schemas/superpowers-bridge-wrspm`, and `nix build --option builders '' '.#homeConfigurations."crs58@aarch64-darwin".activationPackage' --no-link` exits 0
- [ ] 4.4 Build one darwin toplevel and one nixos toplevel to confirm the module edits evaluate and build — verify: `nix build --option builders '' .#checks.aarch64-darwin.darwin-stibnite --no-link` exits 0 locally, and `nix build .#checks.x86_64-linux.nixos-magnetite --no-link` exits 0 through the configured remote builder
- [ ] 4.5 Re-run the hand probe from 1.1 on aarch64-darwin — verify: the offender list is exactly `gitleaks`, and `nix-store -q --requisites $(nix eval --raw .#checks.aarch64-darwin.darwin-stibnite.drvPath) | grep -c -- '-source$'` counts no path equal to `nix flake metadata --json | jq -r .path`

## 5. Add the probe script and recipe

- [ ] 5.1 Add `scripts/check-source-audit.sh` (bash, `set -euo pipefail`) taking an optional system argument defaulting to `nix eval --impure --raw --expr builtins.currentSystem`, reading the allow-list names with `nix eval --json .#lib.checkSourceAllowList | jq -r '.[].name'`, computing the offender set per the 1.1 procedure, printing a `check<TAB>whole-tree|isolated` table, and exiting 1 when the sets differ — verify: `shellcheck scripts/check-source-audit.sh` exits 0 and `scripts/check-source-audit.sh aarch64-darwin` prints a 122-row table with `gitleaks` as the only `whole-tree` row and exits 0
- [ ] 5.2 Add `check-source-audit system=""` to the `nix` group of the `justfile`, delegating to the script — verify: `just check-source-audit` exits 0 on this machine and `just check-source-audit x86_64-linux` exits 0 with `gitleaks` as the only `whole-tree` row among 128
- [ ] 5.3 Prove the probe can fail — verify: with `src = self;` temporarily added to the `home-configurations-exposed` `runCommand` attributes in `modules/checks/validation.nix`, `just check-source-audit` exits 1 with `home-configurations-exposed` marked `whole-tree`; the edit is then reverted

## 6. Integration Verification

- [ ] 6.1 Verify the derivation-hash stability the ADR asks for end to end — verify: record `nix eval --json .#checks.aarch64-darwin --apply 'cs: builtins.mapAttrs (_: c: c.drvPath) cs'` to a file, append a line to `docs/notes/README.md`, re-evaluate to a second file, revert the edit, and `diff` shows `gitleaks` as the only changed `drvPath`
- [ ] 6.2 Verify the assertion pair and the probe agree on both systems — verify: `nix build .#checks.aarch64-darwin.structure-check-source-isolation .#checks.x86_64-linux.structure-check-source-isolation --no-link` exits 0 (linux through the remote builder), and `just check-source-audit aarch64-darwin` and `just check-source-audit x86_64-linux` both exit 0 with `gitleaks` as the only `whole-tree` row
- [ ] 6.3 Validate the change artifacts and confirm scope — verify: `openspec validate --change filter-check-sources-for-substitution` exits 0, and `git diff --name-only <base>..HEAD` contains only `modules/checks/structure/check-source-isolation.nix`, `modules/checks/validation.nix`, `modules/checks/security.nix` (comment only, if touched), `modules/formatting.nix`, the six `modules/home/users/*/default.nix` files, `modules/nixos/hm-sops-bridge.nix`, `modules/home/ai/openspec/default.nix`, `scripts/check-source-audit.sh`, `justfile`, and files under `openspec/changes/filter-check-sources-for-substitution/`
