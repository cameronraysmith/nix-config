## 1. Confirm sources and architectural commitments

- [x] 1.1 Recheck the eleven instruction and skill files named in group 2, `.github/mergify.yml`, and `pkgs/by-name/stack-land/{package.nix,stack-land.sh,test-stack-land.sh}`; read source-versus-delivered contracts before editing and record missing paths rather than guessing replacements — verify: `test -f` succeeds for every expanded path, `modules/home/ai/plugins/` contains no `mergify-stack/SKILL.md`, and the source inventory records all eleven documents separately.
- [x] 1.2 Repeat the ADR-cross-impact audit over `packages/docs/src/content/docs/development/architecture/adrs/` and save `adr-cross-impact.md` in this change; if a decision commits to Mergify's server queue or buildbot as the landing gate, propose explicit supersession and request an operator decision before proceeding with contradictory guidance — verify: enumerate numbered ADRs separately from `index.md`, run `rg -i --no-line-number 'mergify|buildbot|queue|landing'` over the directory, inspect matching decision/consequence sections and the CI ADRs, and report counts plus every hit; planning baseline is 21 numbered ADRs, one index, zero hits.
- [x] 1.3 Capture a pre-edit content matrix for `Queue authorization has one documented owner`, the upstream overrides, and every caller, using the existing source and generated outputs as the red baseline — verify: the matrix reports missing E1/shape rules and current real `stack-land` or Mergify terminal routes rather than treating mere owner-name presence as a pass; record the protected PR 2738/2739/2740 evidence sentence for byte comparison.

## 2. Edit every instruction and skill before App installation

Each item below is its own document task.
For skill delivery checks, `COMPOSE` denotes the output of a fresh `nix build .#apm-skills-compose --no-link --print-out-paths`; inspect the corresponding file under both `$COMPOSE/.claude/skills/` and `$COMPOSE/.agents/skills/`.
Rebuild after each independently verified edit group, never substituting an old installed symlink for fresh evidence.
This whole group lands before App installation; `.github/mergify.yml` retains its queues until group 5.

- [x] 2.1 Update `modules/home/ai/plugins/version-control-and-forge/.apm/skills/git-stacked-pr-integration/SKILL.md` as the sole `Queue authorization` owner defined by the skill-corpus delta, including the first-party upstream override section from the third-party dependency delta; replace the real checked-landing role with asynchronous queue handoff and extend the trigger description for ordinary PRs — verify: the source and freshly composed `git-stacked-pr-integration/SKILL.md` pass every policy-matrix row, the exact Linux warming command appears once in the owner, the no-auto-merge-on-any-member prohibition and poller precedence rationale are explicit, the branch-prefix and never-resync overrides exist, and the protected evidence sentence remains byte-identical with its limited provenance.
- [x] 2.2 Update `modules/home/ai/plugins/agent-context-vcs/.apm/instructions/030-stacked-landing-protocol.instructions.md` to replace “lands the stack through the selected landing mechanism” and the git-native `stack-land` route with authorization through the owner; preserve worker ref return, local jj contracts, and context-tier behavior — verify: source review finds no real landing instruction, `nix eval --raw .#darwinConfigurations.stibnite.config.home-manager.users.crs58.programs.agents-md.settings.body` emits the new route, and full root-context composition contains it while default repository-tier composition does not duplicate the fleet paragraphs.
- [x] 2.3 Update `modules/home/ai/plugins/nix-build-operations/.apm/skills/nix-flake-pr-cycle/SKILL.md` so its description, overview, CI-monitor interpretation, terminal phase, and guardrails route to the owner; connect its warming step to the owner's Linux command and remove Mergify-trigger, author-approved-as-enqueue, and all-buildbot-checks-as-landing-gate claims for this protocol — verify: source and composed `nix-flake-pr-cycle/SKILL.md` contain a resolvable owner-section pointer and warming route, preserve targeted local validation, name nixbot feedback without redefining the queue gate, and contain no prescribed Mergify queue or synchronous author landing wait for queue-managed repositories.
- [x] 2.4 Update `modules/home/ai/plugins/preferences-code-and-collaboration-conventions/.apm/skills/preferences-git-version-control/SKILL.md` with the policy-owner route for external landing while leaving VCS detection and local commit guidance intact — verify: source and composed `preferences-git-version-control/SKILL.md` resolve `git-stacked-pr-integration` §Queue authorization, preserve the mode-detection contract, and do not present local authoring mode as selecting a different external landing mechanism.
- [x] 2.5 Update `modules/home/ai/plugins/preferences-code-and-collaboration-conventions/.apm/skills/preferences-git-version-control/03-jj-mode.md` to route completed verified refs to the owner without changing shared working-copy or worktree handoff discipline — verify: source and composed `preferences-git-version-control/03-jj-mode.md` retain the return-by-ref and local-operation restrictions and add a resolvable external authorization pointer with no direct-trunk landing exception for this queue.
- [x] 2.6 Update `modules/home/ai/plugins/version-control-and-forge/.apm/skills/jj-version-control/SKILL.md` at the external stacked-submission handoff and Mergify Stack-Aware Base references, preserving local join/chain routing and worktree return-by-ref — verify: source and composed `jj-version-control/SKILL.md` route chain-to-ref-to-stack publication and authorization to the owner, retain `Worktree interop` and local hazard contracts, and no longer prescribe the legacy N+1 aggregate or Mergify integration path for queue-managed GitHub repositories.
- [x] 2.7 Update `modules/home/ai/plugins/version-control-and-forge/.apm/skills/jj-version-control/diamond-workflow.md` at external submission/Phase 4 and its Stack-Aware Base footnote; preserve local diamond and linearization mechanics while handing external authorization to the owner — verify: source and composed `jj-version-control/diamond-workflow.md` have no operative “After buildbot passes ... Mergify fast-forward” step for this protocol, retain chain/ref mechanics, and direct any retained generic forge recipe explicitly outside the queue-managed GitHub route.
- [x] 2.8 Update `modules/home/ai/plugins/version-control-and-forge/.apm/skills/jj-version-control/tiered-ceremony.md` so ceremony remains a local-work choice and each externally published change follows the owner; remove direct-trunk, manual fast-forward, and Mergify terminal exceptions for queue-managed repositories — verify: source and composed `jj-version-control/tiered-ceremony.md` distinguish local ceremony from risk class and PR shape, retain tier transitions and return-by-ref, and direct every queue-managed external landing path to the owner.
- [x] 2.9 Update `modules/home/ai/plugins/agent-context-vanixiets/.apm/instructions/10-repository-overview.instructions.md` to replace its human-fast-forward/bot-batch Mergify queue account with a staged transition and policy-owner route; accurately state that Mergify remains configured until the installation window — verify: source plus default and full generated root `AGENTS.md` contain the repository route and transition boundary, and neither promises author-based merge behavior or claims the new App already manages this repository before readiness evidence.
- [x] 2.10 Update `modules/home/ai/plugins/preferences-nix-and-secrets/.apm/skills/preferences-nix-ci-cd-integration/references/buildbot-nix-configuration.md` at `Mergify gating` so historical buildbot examples are explicitly scoped away from the adopted queue and current landing follows the owner — verify: source and composed `preferences-nix-ci-cd-integration/references/buildbot-nix-configuration.md` preserve generic buildbot configuration, resolve the owner pointer, and contain no active Mergify/buildbot landing-gate recommendation for this queue.
- [x] 2.11 Update `modules/home/ai/plugins/preferences-nix-and-secrets/.apm/skills/preferences-nix-ci-cd-integration/references/migration-pattern.md` at the migration summary and Mergify gate example to distinguish legacy migration context from the adopted landing protocol — verify: source and composed `preferences-nix-ci-cd-integration/references/migration-pattern.md` resolve the owner, preserve unrelated migration guidance, and do not make buildbot checks or a Mergify server queue the current landing authority.

## 3. Verify and deliver the pre-install instruction group

- [ ] 3.1 Build the updated corpus and verify all nine skill/reference files from group 2 under both composed targets, including distinct first-party and upstream entry points; compare upstream `mergify-stack/SKILL.md` with its pinned source without editing it — verify: `nix build .#apm-skills-compose --no-link --print-out-paths` succeeds, the per-file content matrix passes on both output trees, the upstream comparison is byte-identical, and `nix build .#checks.aarch64-darwin.structure-mergify-release-alignment .#checks.aarch64-darwin.structure-mergify-release-alignment-neg --no-link` succeeds.
- [ ] 3.2 Exercise generated context from the two instruction fragments without editing output by hand — verify: the stibnite `programs.agents-md.settings.body` evaluation contains the fleet route; `just agents-context` and `just agents-context-full` in disposable copies produce root `AGENTS.md` with the expected repository-only and full-tier routes, respectively, and no tier duplicates or obsolete landing step.
- [ ] 3.3 Have the orchestrator land the complete instruction/skill group before App installation while preserving Mergify's working configuration — verify: the reviewed pre-install revision includes every group-2 document, parsed `.github/mergify.yml` still has queue actions for `default` and `bot-updates` and both definitions, and the App-installation gate remains pending.
- [ ] 3.4 Obtain operator-approved profile delivery before the sibling installation gate; rebuild the relevant configuration and inspect actual harness text after approved activation rather than editing delivered files — verify: `nix build .#darwinConfigurations.stibnite.config.system.build.toplevel --no-link` succeeds, delivered user context and `~/.claude/skills/git-stacked-pr-integration/SKILL.md` carry the new route and owner, and any repository-local `.agents/` path used during cutover has separately generated and inspected post-main bytes; absent activation or stale materialization leaves this task unchecked.

## 4. Demote stack-land through its existing test seam

- [x] 4.1 Extend `pkgs/by-name/stack-land/test-stack-land.sh` before changing production behavior: assert non-dry-run rejection before git/gh calls, no remote-write command traces during dry runs, unchanged fixture target refs, and retained failure cases for ancestry, trailers, and reported-check states — verify: the new cases fail against the current real-landing implementation in isolated fixtures for the expected reasons; fixtures never point at a live remote and the harness records attempted writes, not just resulting ref state.
- [x] 4.2 Update `pkgs/by-name/stack-land/stack-land.sh` to reject operational invocations lacking `--dry-run`, remove real push and merged-state wait paths, and describe assertions only; retain existing read-only diagnostics — verify: `bash -n` and `shellcheck` pass for the script, the focused harness turns green for task 4.1, help describes no real landing mode, and inspection finds no remote mutation path or mandatory queue-gating claim.
- [x] 4.3 Update `pkgs/by-name/stack-land/package.nix::meta.description` for assertion-only use while retaining the package name, runtime inputs needed by diagnostics, and existing test passthru — verify: `nix eval --raw .#packages.aarch64-darwin.stack-land.meta.description` describes assertions, `nix build .#stack-land .#packages.aarch64-darwin.stack-land.tests.integration --no-link` succeeds, and existing home-manager installation wiring remains unchanged.
- [x] 4.4 Review the retired invocation paths with the instruction group and deliver the diagnostic package without a real landing fallback — verify: the complete `test-stack-land.sh` suite passes, dry-run success and each negative case are recorded, and no scoped skill calls the tool as the terminal operation or prerequisite for authorization.

## 5. Coordinate Mergify queue removal with App installation

- [ ] 5.1 Present the operator with the proposed `.github/mergify.yml` semantic diff and the completed pre-install delivery evidence, then obtain the shared cutover window with `stand-up-gitea-mq-on-magnetite`; do not install the App from this task — verify: a cutover record names the operator's approval, group-3 delivery evidence, and the sibling installation gate; without all three, queue removal is held and this task remains unchecked.
- [ ] 5.2 In that approved installation window, remove `.github/mergify.yml` queue actions `queue human PRs` and `queue bot PRs`, definitions `default` and `bot-updates`, the unused `merge_queue` block, and orphan queue-only anchors; preserve unrelated assignment and review rules — verify: YAML parsing succeeds, `yq -e 'has("queue_rules") or has("merge_queue")' .github/mergify.yml` is false, no `pull_request_rules[].actions` has a `queue` key, and surviving rule names and bodies equal the pre-cutover `self-assign PRs` and `auto-approve owner-authored PRs with label` rules.
- [ ] 5.3 Have the orchestrator land the queue-removal group in the same window as the sibling App installation, and keep author authorization held until the sibling reports installation readiness — verify: the cutover record associates the removal revision and timestamp with operator installation evidence; read-only sibling evidence confirms the repository is managed, both external nixbot checks are derived from the operator ruleset, and the separate App gate is active; no service, ruleset, or App mutation is performed under this task.

## Integration Verification

- [ ] 6.1 Re-run the complete affected verification suite after all source changes: OpenSpec structure, all eleven per-document checks, both composed targets, both root-context modes, rendered user context, release-alignment positive/negative checks, stack-land integration suite, and parsed Mergify YAML — verify: `openspec validate adopt-queue-landing-protocol --strict` passes alongside every named group-2 through group-5 check, with exact commands, output paths, and failures retained; broad repository typechecking is not a substitute for these delivery and effect checks.
- [ ] 6.2 Review standards and spec conformance and record the satisfaction boundary in the implementation verification artifact — verify: each requirement maps to its source and output evidence, every retired route has a negative content check, the ADR audit count and hits are present, and no result claims that text inspection or OpenSpec validation proves E1 compliance or upstream behavior; §8 review is agent-executed and non-blocking, not structural validation, and records the absence of a change-local world-assumptions delta while identifying the canonical designation table.
- [ ] 6.3 After operator-approved cutover, audit available authorization samples across both risk classes and both PR shapes without issuing new signals or requiring authors to wait — verify: read-only PR records identify review-before-signal where required, native registration and selected-head ancestry for stacks, no stack-member auto-merge, and queue comments for asynchronous rejection; any unsampled class or shape is explicitly unverified and queued for follow-up, never reported as queue-enforced review.
- [ ] 6.4 Confirm scope and rollback safety before acceptance — verify: the change edits only the named first-party guidance, assertion tool, Mergify configuration, and planning/evidence artifacts; upstream skill, service deployment, check-source implementation, and local jj return-by-ref contracts are unchanged, and the cutover record requires operator coordination before restoring any old queue configuration.
